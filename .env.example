# Optional — leave as placeholder to use mock search results
VITE_NEWS_API_KEY=your_news_api_key_here

# Backend URL for local dev (leave empty in production when served from same domain)
VITE_API_BASE_URL=http://localhost:3001
